# RIKA Lens Calculator

Калькулятор подбора объектива для тепловизионных прицелов.

Находит фокусное расстояние, при котором 1 мрад точно укладывается в целое число пикселей микродисплея по обеим осям (H и V). Это устраняет «плавание» штрихов прицельной сетки, вызванное дробным округлением.

**Live:** https://shah1git.github.io/rika-lens-calc/

## Стек

- Vite + React + TypeScript
- Recharts (графики)
- GitHub Pages (деплой через GitHub Actions)
- Без CSS-фреймворков — inline styles + минимальный `global.css`

## Структура

```
src/
  App.tsx       — единственный компонент: логика, UI, i18n
  main.tsx      — точка входа
  global.css    — reset + CSS-тултипы для таблицы
index.html      — подключение JetBrains Mono, lang=ru
vite.config.ts  — base: /rika-lens-calc/
```

## Разработка

```bash
npm install
npm run dev     # dev-сервер
npm run build   # сборка в dist/
```

## Деплой

Автоматический через `.github/workflows/deploy.yml` при пуше в `main`.

## Функциональность

- **Пресеты сенсоров:** 256x192, 384x288, 640x480, 640x512, 1024x768, 1280x1024
- **Пресеты дисплеев:** 640x480, 1024x768, 1280x1024, 1920x1080, 2560x2560
- **Шаг пикселя:** 12, 15, 17, 25 µm
- **Диапазон фокусных:** 5–200 мм, настраиваемый
- **i18n:** RU / EN / ZH, переключатель флагами, сохранение в localStorage
- **ExplainBlock:** объяснение формулы `max(H, V)` с живым примером из текущих данных
- **findMultiples:** кратные фокусные для 0% ошибки по каждой оси
- **График:** столбчатая диаграмма ошибок с цветовой кодировкой (зелёный < 1%, жёлтый < 5%, красный > 5%)
- **Таблица:** сортировка по итоговой ошибке, топ-5 подсвечены

## Версионирование

Версия отображается в заголовке рядом с названием (`v3.0`, `v3.1`, ...).
При каждом коммите версия инкрементируется:
- **patch** (3.0 → 3.1) — баг-фиксы, мелкие правки
- **minor** (3.x → 4.0) — новые блоки, фичи, существенные изменения

## Changelog

### v4.5
- Переписаны тултипы колонок основной таблицы (tipF, tipPpm*, tipErr*, tipWorst, tipMm*) — формулы, пороги цвета, примеры реального промаха
- Переписаны описания колонок (descF..descMm) — EN были куцые, ZH были пустые
- Подробные переводы title/subtitle на EN и ZH (были сильно сокращены)
- Подробные тултипы на всех элементах таблиц дистанций: заголовки с формулами, колонки с описанием физического смысла, каждая ячейка с динамическим пояснением
- Тултипы в раскрывающихся строках и панели сравнения
- Вспомогательная функция cTip для генерации контекстных тултипов (pixH/pixV/errH/errV/dist) на 3 языках

### v4.4
- Раскрывающиеся строки: клик по строке раскрывает две таблицы дистанций (размер пикселя + ошибка позиции)
- Кнопка «Добавить в сравнение» внутри раскрытой строки (вместо клика по строке)
- Нативные title-тултипы на заголовках колонок, кнопках приоритета, строках и ячейках ошибок
- Подсказка «Кликните на строку» под заголовком таблицы
- Пояснения под таблицами в раскрытой строке и карточках сравнения

### v4.2
- Таблица ошибки позиции метки в карточках сравнения (err% × D, цветовая кодировка)

### v4.1
- Восстановлена валидация фокуса на onBlur (draft-строка в Nm)
- Лимит сравнения увеличен до 9 позиций

### v4.0
- URL sharing: все параметры сохраняются в hash, кнопка «Скопировать ссылку»
- Сравнение конфигураций: клик по строке добавляет в панель сравнения (макс. 3)
- Таблица размера пикселя по дистанциям (100–1000 м) в карточках сравнения

### v3.5
- Убраны цветные кружочки из первой колонки — теперь только jackpot ✦ для ideal-строк

### v3.4
- Восстановлены подробные тултипы заголовков таблицы (RU/EN), заполнены ZH

### v3.3
- Jackpot-индикатор: пульсирующая звёздочка ✦ и бейдж IDEAL для строк с ошибкой < 0.01% по обеим осям
- Восстановлены CSS-тултипы таблицы (data-tip вместо title)
- Текущий режим приоритета осей отображается рядом с заголовком таблицы

### v3.2
- Восстановлен пресет сенсора 256x192 и дисплея 2560x2560 (потеряны в v3.0)
- Восстановлена версия в заголовке

### v3.1
- Добавлен пресет сенсора 256x192

### v3.0
- Добавлен селектор приоритета осей: «Обе оси равны» / «Приоритет V» / «Только V»
- Режим «Обе оси» — `score = max(H, V)`, как раньше
- Режим «Приоритет V» — сортировка по V, при равных V — лучший H
- Режим «Только V» — `score = V`, горизонталь серая и не влияет на рейтинг
- Блок объяснения (`Explain`) адаптируется под выбранный режим
- Рефакторинг: `AxisResult` упрощён (убраны `mpp`, `near`, `r`), `RowResult.worst` → `score`
- Компоненты переименованы: `Card`→`Cd`, `ParamBlock`→`PB`, `Num`→`Nm`, `LangSwitcher`→`LangSw`

### v2.0
- Добавлен `ExplainBlock` — блок «Как читать таблицу» с живым примером
- Добавлена `findMultiples` — показ кратных фокусных для 0% ошибки по каждой оси
- Колонка «Худш.» → «Итог.» с пояснением `max(H, V)`
- Обновлены все i18n-ключи (ru/en/zh)
- Добавлена метка версии в заголовок

### v1.x (начальная разработка)
- Базовый калькулятор с графиком и таблицей
- Добавлен пресет дисплея 2560x2560
- Нативные `title`-тултипы заменены на CSS-тултипы (`data-tip` + `::after`) — мгновенное появление при наведении
- Валидация инпутов фокуса перенесена с `onChange` на `onBlur` — свободное редактирование, clamp при потере фокуса или Enter
- CI: GitHub Pages deploy workflow
